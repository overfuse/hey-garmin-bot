version: "3.8"

services:
  mongo:
    image: mongo:6.0
    container_name: garmin-mongo
    restart: unless-stopped
    volumes:
      - ./mongo_data:/data/db
    ports:
      - "27017:27017"

  redis:
    image: redis:7-alpine
    container_name: garmin-redis
    restart: unless-stopped
    ports:
      - "6378:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: garmin-bot
    restart: unless-stopped
    depends_on:
      - mongo
      - redis
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: "1"
      # "curl": curl_cffi w/ Chrome TLS impersonation, bypasses Cloudflare JA3.
      # "garth": plain requests (works only from residential IPs).
      # See garmin.py LOGIN_METHOD docstring.
      GARMIN_LOGIN_METHOD: "curl"
      MONGODB_URI: "mongodb://mongo:27017"
      REDIS_URL: "redis://redis:6379/0"
    command: ["python", "bot.py"]
