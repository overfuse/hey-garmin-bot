version: "3.8"

services:
  mongo:
    image: mongo:6.0
    container_name: garmin-mongo
    restart: unless-stopped
    volumes:
      - ./mongo_data:/data/db
    ports:
      - "27017:27017"

  redis:
    image: redis:7-alpine
    container_name: garmin-redis
    restart: unless-stopped
    ports:
      - "6378:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: garmin-bot
    restart: unless-stopped
    depends_on:
      - mongo
      - redis
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: "1"
      # "web": user logs in to Garmin in their own browser and pastes the
      # post-login ?ticket=ST-... URL back into the chat (no creds to bot,
      # no Cloudflare block). See garmin.py LOGIN_METHOD docstring.
      GARMIN_LOGIN_METHOD: "web"
      MONGODB_URI: "mongodb://mongo:27017"
      REDIS_URL: "redis://redis:6379/0"
    command: ["python", "bot.py"]
