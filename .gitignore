.idea
.venv
.env
.DS_Store
mongo_data
__pycache__

# Pyrogram Telegram session (contains bot auth keys — never commit)
*.session
*.session-journal

# wrangler local cache (Cloudflare account id/name — not for a public repo)
.wrangler/

# eval run output — regenerated and nondeterministic on every `python -m evals.run`
evals/last_results.json

# working notes / implementation plans — scratch, not a record of the codebase
.plans/